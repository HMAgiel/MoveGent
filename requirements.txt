# MovGent runtime requirements (hand-curated from actual imports, NOT pip freeze)
# Python >=3.12

# --- LLM / orchestration ---
langchain>=1.2.15
langchain-community>=0.4.1
langchain-core>=1.3.0
langchain-openai>=1.2.0
langchain-qdrant>=1.1.0
langgraph>=1.1.9
langfuse>=4.5.0

# --- Vector / retrieval ---
qdrant-client>=1.17.1
sentence-transformers>=5.4.1
huggingface-hub>=1.11.0

# --- Data / storage ---
sqlalchemy>=2.0.49
pandas>=3.0.2
requests                      # direct import in chatbot/tools/tool.py (OMDB_tool)

# --- UI / app ---
streamlit>=1.57.0
tiktoken>=0.12.0

# --- Env / typing ---
python-dotenv                  # NOTE: pyproject lists "dotenv>=0.9.9" - that name is the abandoned
                               # PyPI package; load_dotenv comes from python-dotenv
typing-extensions              # direct import in chatbot/graph/state.py

# --- Notebook only (eksperiment.ipynb) ---
ipykernel>=7.2.0
tqdm>=4.67.3

# Not listed (intentionally):
# - langchain-deepseek / langchain-huggingface: in pyproject but not imported anywhere in code
# - uuid: stdlib module; the PyPI "uuid" pin in pyproject is unnecessary
# - langchain_classic: transitive dep of langchain>=1.2.15 (hub.pull in SQL_agent)
